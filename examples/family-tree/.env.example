# Connection string used by `prisma-next db init`, the seed script, and the app.
# Matches docker-compose.yml (port 5434). Copy this file to `.env`.
DATABASE_URL=postgres://postgres:postgres@localhost:5434/family_tree
